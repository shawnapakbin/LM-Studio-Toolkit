# Normalize text files to LF in-repo to avoid cross-platform formatter churn.
* text=auto eol=lf

# Keep Windows scripts with CRLF for native shell ergonomics.
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf
