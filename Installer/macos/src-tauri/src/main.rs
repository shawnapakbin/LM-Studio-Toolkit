// macOS skeleton — see Installer/macos/README.md for the plan.
// This file exists so tooling can find the crate layout; it is excluded
// from the Cargo workspace and not built on Windows CI.

fn main() {
    println!("LLM Toolkit Installer (macOS) — not yet implemented.");
}
